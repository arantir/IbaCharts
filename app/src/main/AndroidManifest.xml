<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mychartsapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Убрали MANAGE_EXTERNAL_STORAGE - это опасное разрешение, используйте только если необходимо -->
    <!-- <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" /> -->

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".NormalChartActivity"
            android:exported="false" />
            
        <activity
            android:name=".IBAChartActivity"
            android:exported="true" >
            
            <!-- Сохраняем intent-filter для открытия .dat файлов -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/octet-stream" />
                <data android:scheme="content" />
                <data android:host="*" />
            </intent-filter>
            
            <!-- Альтернативный фильтр для прямой работы с файлами -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.dat" />
                <data android:mimeType="*/*" />
            </intent-filter>
            
        </activity>
            
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
            
        <activity
            android:name=".NormalChartSettingsActivity"
            android:exported="false" />
            
        <activity
            android:name=".IBAChartSettingsActivity"
            android:exported="false" />
    </application>

</manifest>